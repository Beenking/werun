<!--logs.wxml-->
<view class="container step-list">
   <button class="usermotto" bindtap='getStepData'>
    <text class="user-motto">{{motto}}</text>
  </button>
  <block wx:for="{{steps}}" wx:key="*this">
    <text class="step-item">{{index + 1}}. {{item.date}}: {{item.step}}步</text>
  </block>
</view>
 
